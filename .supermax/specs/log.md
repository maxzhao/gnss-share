# Specifications Log

- 2026-09-12: Initialized specification entrypoint.
- 2026-09-12: Added draft change workspace `changes/replace-wifi-with-bluetooth/` with proposal, design, delta behavior spec, implementation checklist, and lifecycle index entry. Source: confirmed user requirements plus current Android manifests, services, connection code, preferences, and `proto/location.proto`. Validation: artifact structure and references checked; implementation/build/device validation not run.
- 2026-09-12: Implemented the draft Bluetooth change and recorded automated evidence. `ANDROID_HOME="$HOME/Android/Sdk" ANDROID_SDK_ROOT="$HOME/Android/Sdk" ./gradlew clean assembleDebug lintDebug`, `git --no-pager diff --check`, removed-feature scanning, and independent code review passed. Human phone/tablet validation remains not run, so the change stays `status: draft` and no stable spec or `.supermax/AGENTS.md` merge was performed.
- 2026-09-13: Added and implemented draft change workspace `changes/add-inertial-dead-reckoning/` after source, Vault and upstream-library exploration plus user agreement. It specifies fixed-source `mad-location-manager-lib` integration, latest real-location startup without an age limit, 500 ms unlimited prediction, age-aware growing accuracy, unsupported-sensor fallback and the existing 15-second disconnected-client stop lifecycle. `ANDROID_HOME="$HOME/Android/Sdk" ANDROID_SDK_ROOT="$HOME/Android/Sdk" ./gradlew assembleDebug lintDebug`, recursive submodule checks, native ABI checks and `git --no-pager diff --check` passed；all high/medium findings from independent code review were repaired. Human phone/tablet validation remains not run, so the change stays `status: draft` and is not merged into stable specifications.
- 2026-09-13: Added draft change workspace `changes/add-service-controls/` after repository/Vault exploration, Android official Quick Settings and foreground-service restriction review, and user agreement. It specifies per-app toggle Tiles, lifecycle-synchronized state, cold-process Activity launch, prerequisite fallback, in-app Start/Stop buttons, same-visible-cycle manual stop, and retained later-`onStart` automatic startup. The change partially supersedes only the Bluetooth draft's prohibition on in-app service controls.
- 2026-09-13: Implemented the initial `changes/add-service-controls/` convergence with independent-process Tiles, `ACTIVE_TILE=true`, `TOGGLEABLE_TILE=true`, an own-service-record state query, Android 14+ conservative location-FGS routing, warm `singleTask` Activity fallback through a private `onNewIntent()` start marker, explicit stopped rendering, and lifecycle refresh requests. That implementation passed its recorded automated/static checks but was later superseded inside the same draft workspace by the target-device PID-marker synchronization repair below. Human phone/tablet acceptance remains incomplete, so the change stays `status: draft`, `merged_to: []`, and unarchived.
- 2026-09-13: Repaired the independent service-controls review finding for secure lock-screen startup. Active Tiles still stop immediately; inactive Tiles now defer through `TileService.unlockAndRun()` when `isLocked() && isSecure()`, preserve click-time main-process existence, and re-check that the service remains stopped before direct start or Activity fallback. Focused Java compilation plus both `lintDebug` tasks passed (`BUILD SUCCESSFUL`, 98 actionable tasks); final `ANDROID_HOME="$HOME/Android/Sdk" ANDROID_SDK_ROOT="$HOME/Android/Sdk" ./gradlew assembleDebug lintDebug`, `git --no-pager diff --check`, `proto/location.proto` no-diff and APK existence checks passed (`BUILD SUCCESSFUL`, 146 actionable tasks; `final-validation: passed`). Human secure-lock behavior remains not run, so lifecycle status remains draft.
- 2026-09-13: Repaired target-device stale visual state in both service-control Tiles. Root cause: lifecycle refreshes reached the active Tile contract, but independent Tile processes re-read delayed/OEM-dependent `ActivityManager.getRunningServices()` records and the stop path guessed record-removal timing. Each service now atomically publishes its current main-process PID after successful foreground creation, deletes the app-private marker at destruction start, and requests Tile listening refresh after each transition; Tile state is active only when the marker PID equals the live default-main-process PID and remains independent of Bluetooth, peer and GNSS flow. Focused Java compilation/lint and cross-process lifecycle-state source proof passed; final compile/lint/assemble plus diff/protocol checks passed (`BUILD SUCCESSFUL`, 143 actionable tasks; `final-validation: passed`). Corrected target-device retest remains not run, so lifecycle status stays draft.
